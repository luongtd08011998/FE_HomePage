# TV — Hero carousel (trang chủ)

Mã nguồn đã tách khỏi file này và nằm tại:

- `src/components/home/TvNewsHero.tsx` — carousel (Motion, Lucide: Tag, ChevronLeft/Right)
- `src/components/home/tvNewsHeroData.ts` — 5 slide tin mẫu (ảnh Unsplash)

Font tiêu đề slide: **Cormorant Garamond** — biến CSS `--font-cormorant` trong `app/layout.tsx`.

Trang chủ import: `app/page.tsx` (khối HERO).
