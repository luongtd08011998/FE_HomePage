"use client";

// View increment endpoint not yet available in backend.
// This component is a placeholder for when POST /articles/:id/view is added.
export default function ViewCounter({ articleId: _ }: { articleId: number }) {
  return null;
}
